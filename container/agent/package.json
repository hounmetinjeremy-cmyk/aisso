{
  "name": "aisso-container-agent",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Agent exécuté dans le conteneur distant : fs + exec + watch + détection de ports, exposés en WebSocket au Worker Cloudflare.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "chokidar": "^3.6.0",
    "minimatch": "^9.0.5"
  }
}
